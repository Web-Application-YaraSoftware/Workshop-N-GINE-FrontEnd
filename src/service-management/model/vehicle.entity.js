export class Vehicle {
    constructor(id, user_id, license_plate, brand, model) {
        this.id = id;
        this.userId = user_id;
        this.licensePlate = license_plate;
        this.brand = brand;
        this.model = model;
    }
}